fastapi==0.115.0
uvicorn==0.32.0
python-jose[cryptography]==3.3.0
bcrypt==4.1.3
python-multipart==0.0.17
neo4j==6.1.0
chromadb==0.4.18
numpy==1.26.4
firecrawl-anydoc==0.2.4
jieba==0.42.1
python-dotenv==1.0.0
aiosqlite==0.20.0
rank-bm25==0.2.2
# --- URL ingestion (FEAT-019): readability-lxml pulls lxml (already present
# transitively), chardet, cssselect, lxml_html_clean; html2text is pure-Python.
readability-lxml==0.9
html2text==2025.4.15
# --- version-aligned stack for the app + MCP server (GUIDE-003 T2-3) ---
# Pinned together: mcp 2.x drags starlette>=1.0 which breaks fastapi 0.115
# (on_startup removed); pydantic 2.13 required by mcp 1.29.
httpx==0.28.1
starlette==0.38.6
pydantic==2.13.4
pydantic-settings==2.11.0
mcp==1.29.0

# --- dev / test only (install with: pip install -r requirements-dev.txt or pip install pytest pytest-asyncio) ---
pytest==8.3.3
pytest-asyncio==0.24.0
